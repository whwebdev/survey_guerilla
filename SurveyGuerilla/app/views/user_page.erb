<h1><%= @user.username %></h1>
<div id="created_surveys">
  <ol>
    <% @user.created_surveys.each do |survey| %>
      <li><%= survey.name unless survey.nil? %></li>
    <% end %>
  </ol>
</div>
  <ol>
    <% @user.taken_surveys.each do |survey| %>
      <li><%= survey.name unless survey.nil? %></li>
    <% end %>
  </ol>
</div>
